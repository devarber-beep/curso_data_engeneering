version: 2

sources:

  - name: snapshot # name of the source 
    schema: ejemplo_snapshot # this is the schema our raw data lives in
    database: ALUMNO6_DEV_BRONZE_DB # this is the name of our database

    tables:

      - name: users
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema